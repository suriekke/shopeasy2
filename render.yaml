services:
  - type: web
    name: shopeasy-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run render-start
    rootDir: .
    healthCheckPath: /health
    autoDeploy: true
    # Updated configuration for proper backend deployment
